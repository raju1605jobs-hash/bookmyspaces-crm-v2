# COMPLETED_ISSUES.md

Issues fully implemented and verified during roadmap execution. Cross-reference `MASTER_ISSUE_REGISTER.csv` for full original detail.

| Issue | Title | Files Changed | Verification | Date |
|---|---|---|---|---|
| ISS-040 | Debug logging block dumps payment/invoice data in production | `src/app/api/proposals/[id]/invoice/route.ts` | TypeScript 0 errors, ESLint clean, diff confirms only intended 5 lines removed | 2026-07-11 |
| ISS-031 | No automated testing of any kind exists in the project | `package.json`, `vitest.config.ts` (new), `src/lib/lead-scorer.test.ts` (new) | TypeScript 0 errors, ESLint clean, 11/11 tests passing, diff confirms only 2 intended lines changed in package.json | 2026-07-11 |
| ISS-025 | Stray root files unverified (`git`, `npm`, `latest.json`, `logs.json`, etc.) | `latest.json`, `logs.json` (emptied), `.gitignore` (updated), git history rewritten (local, verified) | Confirmed no full secrets; confirmed committed customer PII in 2 files; git-filter-repo rewrite verified zero references to the 4 PII-bearing files across all history; unaffected branches confirmed byte-identical hash before/after | 2026-07-11 |
| ISS-042 | Unawaited fire-and-forget DB write with swallowed error | `src/app/api/proposals/email/route.ts` | TypeScript 0 errors, ESLint clean; update now awaited and errors logged | 2026-07-11 |
| ISS-047 | WhatsApp auto-reply hardcoded pricing mismatched to real packages | `src/app/api/whatsapp/webhook/route.ts` | TypeScript 0 errors, ESLint clean; pricing reply now queries live `packages` table with a safe fallback | 2026-07-11 |
| ISS-020 | Two API route files misnamed, never registered as Next.js routes | `src/app/api/leads/[id]/stage/route.ts` (new), `src/app/api/proposal/share/[token]/route.ts` (new) | TypeScript 0 errors, ESLint clean; both routes now register correctly. Old misnamed files annotated as superseded but not deleted (sandbox mount does not permit file deletion — flagged for user to `rm` directly) | 2026-07-11 |
| ISS-019 | lead-stage-manager.ts reachable only through dead, misnamed route | (see ISS-020 — same fix) | Confirmed `leads.lead_stage` exists live (SCHEMA_DRIFT_REPORT.md Category C); route now reachable; module logic reviewed, no changes needed | 2026-07-11 |
| ISS-032 | Two Tailwind config files with divergent font-family definitions | `tailwind.config.ts` (annotated as dead) | Confirmed `tailwind.config.js` is the file Tailwind's own resolver (`resolveConfigPath.js`) loads first; both currently render identical fonts via `globals.css` CSS variables, confirmed coincidental not structural | 2026-07-11 |
| ISS-001 | Middleware performed no real session check — any request could reach protected pages | `src/middleware.ts` (rewritten) | TypeScript 0 errors; real `supabase.auth.getUser()` check via `createMiddlewareAuthClient`; public allowlist (`/`, `/auth/*`, `/proposals/share/*`); unauthenticated page requests redirect to `/auth/login?redirect=<path>`. Live-tested — real login and logout confirmed working | 2026-07-11 |
| ISS-003 | Admin routes checked role via raw `user.role !== 'admin'` instead of a real authorization helper | `src/app/api/admin/users/route.ts` | TypeScript 0 errors; GET now uses `requireRole(['admin','manager'])`, PATCH/POST use `requireRole(['admin'])` | 2026-07-11 |
| ISS-004 | WhatsApp webhook accepted unsigned requests from anyone | `src/app/api/whatsapp/webhook/route.ts` | TypeScript 0 errors; HMAC-SHA256 `X-Hub-Signature-256` verification added (`verifySignature()`), fails closed (403) when `WHATSAPP_APP_SECRET` is set and invalid, fails open (logged) only when the secret is unconfigured | 2026-07-11 |
| ISS-033 | Server Actions `allowedOrigins` was `['*']`, allowing CSRF from any origin | `next.config.js` | TypeScript 0 errors; origins restricted to `bookmyspaces.in`, `www.bookmyspaces.in`, `*.vercel.app`, `localhost:3000` | 2026-07-11 |
| ISS-002 (batch 1) | No session/auth check on core dashboard/leads/analytics API routes | `src/lib/auth-guard.ts` (new shared helper), `analytics/route.ts`, `dashboard/revenue/route.ts`, `dashboard/stats/route.ts`, `leads/route.ts`, `leads/hot/route.ts`, `leads/summary/route.ts` | TypeScript 0 errors, ESLint clean; `requireAuth()` added to every handler | 2026-07-11 |
| ISS-002 (batch 2) | No session/auth check on proposals API routes | `proposals/[id]/invoice`, `[id]/receipt`, `[id]/payment`, `proposals/route.ts`, `proposals/intelligence/route.ts`, `proposals/email/route.ts` | TypeScript 0 errors, ESLint clean, 11/11 tests passing; `requireAuth()` added everywhere except confirmed-public `[id]/pdf`, `[id]/preview`, `track-view` (traced actual callers before excluding) | 2026-07-11 |
| ISS-002 (batch 3) | No session/auth check on whatsapp/campaigns/misc API routes | `whatsapp/campaigns/route.ts`, `whatsapp/send/route.ts`, `campaigns/route.ts`, `conversations/route.ts`, `followups/route.ts`, `knowledge/route.ts`, `leads/[id]/stage/route.ts` | TypeScript 0 errors, ESLint clean, 11/11 tests passing; `requireAuth()` added everywhere except confirmed-public `chat/route.ts` (anonymous visitor chatbot) and out-of-scope stub `ai-summary/route.ts` (ISS-016, blocked separately) | 2026-07-11 |
| ISS-027 | Two independent OAuth/magic-link callback implementations diverge in behavior | `src/lib/auth-callback.ts` (new shared handler), `src/app/api/auth/callback/route.ts`, `src/app/auth/callback/route.ts` (both now delegate to the shared handler) | TypeScript 0 errors, ESLint clean, 11/11 tests passing; both URL paths kept alive (unknown which is externally configured in Supabase) and now behave identically | 2026-07-11 |
| ISS-028 | OAuth failure redirect target (`/auth/auth-code-error`) does not exist (404) | (same files as ISS-027) plus `src/app/auth/login/page.tsx` (now reads `?error=callback_failed` and shows a real message) | TypeScript 0 errors, ESLint clean; failure path now redirects to the existing `/auth/login` page with a visible error message instead of 404ing | 2026-07-11 |
| ISS-017 | `escalation-engine.ts` fully implemented but never called from anywhere | `src/app/api/cron/escalations/route.ts` (new), `vercel.json` (registered function + cron entry, every 6 hours) | TypeScript 0 errors, ESLint clean, 11/11 tests passing; mirrors the existing `cron/followups` CRON_SECRET-guarded pattern; fields selected confirmed live against SCHEMA_DRIFT_REPORT.md | 2026-07-11 |
| (new fix, found via manual testing) | Missing sign-out button — no way to log out of the CRM at all | `src/components/layout/CRMLayout.tsx` | TypeScript 0 errors, ESLint clean, live-tested: click "Sign out" correctly ends the session and redirects to login | 2026-07-11 |
| ISS-041 | Proposal 'email' feature returned a `mailto:` link instead of actually sending email | `src/lib/email/provider.ts`, `src/lib/email/templates.ts`, `src/lib/email/send.ts` (new email system, Resend-based), `src/app/api/proposals/email/route.ts` (rewritten), `src/app/(crm)/proposals/page.tsx` (Email button now calls the real route — was previously bypassing the backend entirely with its own inline mailto), `supabase/migrations/011_email_log.sql` (new delivery-log table), `.env.example` | TypeScript 0 errors, ESLint clean, 11/11 tests passing; user ran migration 011, signed up for Resend, configured `RESEND_API_KEY`; falls back gracefully to mailto: if no provider configured | 2026-07-12 |
| (new capability) | Added invoice, payment-reminder, follow-up, and booking-confirmation emails | `src/app/api/proposals/[id]/invoice/email/route.ts`, `src/app/api/proposals/[id]/payment-reminder/route.ts`, `src/app/api/proposals/[id]/booking-confirmation/route.ts`, `src/app/api/leads/[id]/follow-up-email/route.ts` (all new) | TypeScript 0 errors, ESLint clean; all use the same shared `src/lib/email/` system as ISS-041; all require staff auth | 2026-07-12 |
| (new fix, found via live testing) | Every `requireAuth()`/`requireRole()`-protected API route (~25+ routes, all of ISS-002) silently always returned 401, regardless of real login state | `src/lib/supabase-server.ts` (cookie interface rewritten from `getAll`/`setAll` to `get`/`set`/`remove`, matching the installed `| (new fix, found via code audit -- no browser access this session) | Kanban board and Dashboard tracked lead stage via two different, unsynced DB columns (`status` vs `lead_stage`) -- moving a card in one view silently did not move it in the other | `src/app/(crm)/kanban/page.tsx` | TypeScript 0 errors, ESLint clean, 11/11 tests passing; standardized on `lead_stage`, drag-and-drop now calls the validated `/api/leads/[id]/stage` endpoint; could not live-test the drag interaction itself (no browser access this session) | 2026-07-12 |
| ISS-026 | Confirm no duplicate `middleware.ts` | `middleware.ts` (annotated, not deleted) | Confirmed via `.next/server/middleware-manifest.json` after a build -- only `src/middleware.ts` is registered; root file is dead code | 2026-07-12 |
| ISS-029 | `ADMIN_EMAIL`/`ADMIN_PASSWORD` declared but unused | `.env.local`, `.env.example` | Confirmed zero references in `src/`; commented out (values preserved) with explanation | 2026-07-12 |
| ISS-034 (confirmed, doc was stale) | Security headers | `next.config.js` (already done, no new change) | Already implemented (X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy); OPEN_ISSUES.md hadn't caught up | 2026-07-12 |
| ISS-036/050 | No centralized env var config or startup validation | `src/lib/env.ts` (new), `src/instrumentation.ts` (new) | TypeScript 0 errors, ESLint clean, 11/11 tests passing; could not run a full `npm run build` in this sandbox to observe actual startup log output (45s tool-call ceiling) -- recommend the user verify once | 2026-07-12 |
| ISS-037 (remaining gap) | `.env.example` had one leftover unused var reference | `.env.example` | Found already substantially fixed by a prior session; removed the one remaining unused `NEXTAUTH_SECRET` reference. New finding: `CRON_SECRET`/`WHATSAPP_APP_SECRET` correctly documented but missing from `.env.local`, leaving cron + WhatsApp webhook signature checks unauthenticated in production (both fail-open by design) | 2026-07-12 |
| ISS-038 | 5 declared env vars completely unused | `.env.local`, `.env.example` | Confirmed 4 (`ADMIN_EMAIL`, `ADMIN_PASSWORD`, `NEXTAUTH_SECRET`, `NEXT_PUBLIC_BUSINESS_PHONE`) via grep; commented out, values preserved. Generated and added a real `CRON_SECRET` to `.env.local` (was completely missing) -- user still needs to set the same value in Vercel | 2026-07-12 |
| ISS-008 | Stale migration file recreates anon policies no longer live | `supabase/migrations/005_stability_patch.sql` | Confirmed via migration 009's own defensive DROP POLICY statements that `conversations_anon_insert`/`conversations_anon_update` are absent from live production; commented out (not deleted) with explanation | 2026-07-12 |
| ISS-005 (partial, scoped rollout) | No input validation (zod) on any of 31 API routes | `src/lib/validation.ts` (new), `src/lib/validation.test.ts` (new, 13 tests), `src/app/api/leads/route.ts`, `src/app/api/leads/[id]/stage/route.ts` | TypeScript 0 errors, ESLint clean, 24/24 tests passing (up from 11). Applied to the 3 highest-write-risk routes only; found and fixed a real mass-assignment gap in `PATCH /api/leads` (unrestricted `...updates` spread could set `lead_stage` directly, bypassing the validated state machine). Remaining ~28 routes are follow-up work, not claimed done | 2026-07-12 |
